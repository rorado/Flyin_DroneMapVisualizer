# Drone Map Visualizer

An interactive Next.js app for parsing drone map text configs and rendering them as a clean SVG visualization.

## Features

- Next.js App Router + TypeScript + Tailwind CSS
- Text parser with validation
- Interactive SVG map with hover highlights
- Sample maps: easy, medium, hard
- Copy parsed JSON and download PNG

## Run locally

```bash
npm install
npm run dev
```
